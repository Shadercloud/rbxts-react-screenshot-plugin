import React from "@rbxts/react";

export function Lib() {
	return (
		<frame
			key="Lib"
			Size={UDim2.fromOffset(50, 50)}
			BackgroundColor3={Color3.fromRGB(10, 20, 30)}
			BorderSizePixel={0}
		/>
	);
}
