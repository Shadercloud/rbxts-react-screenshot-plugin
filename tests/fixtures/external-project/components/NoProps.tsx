import React from "@rbxts/react";

function NoProps() {
	return (
		<frame
			key="Content"
			Size={UDim2.fromOffset(200, 100)}
			BackgroundColor3={Color3.fromRGB(30, 33, 40)}
			BorderSizePixel={0}
		/>
	);
}

export = NoProps;
